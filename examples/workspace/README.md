# Example workspace

The workflow examples use this folder as their isolated workspace. The included scripts default to dry-run mode, so running them does not modify this folder unless you explicitly disable `dry_run` in a script.
